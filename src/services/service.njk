---
pagination:
  data: services.items
  size: 1
  alias: service
permalink: "/services/{{ service.slug }}/"
layout: base.njk
sitemap: false
robots: "noindex, follow"
eleventyComputed:
  title: "{{ service.title }}"
  description: "{{ service.summary | stripHtml }}"
---

<div class="mx-auto max-w-5xl px-4 py-10">
  <article class="space-y-4">
    <header class="space-y-2">
      <p class="text-sm text-slate-500">
        <a class="hover:underline underline-offset-2" href="{{ '/services/' | url }}">Services</a>
        <span aria-hidden="true">/</span>
        <span>{{ service.title }}</span>
      </p>
      <h1 class="text-3xl font-semibold tracking-tight">{{ service.title }}</h1>
      <p class="rich-content text-slate-700">{{ service.summary | safe }}</p>
    </header>

    <div class="rich-content prose prose-slate max-w-none">
      {{ service.body | safe }}
    </div>
  </article>
</div>

